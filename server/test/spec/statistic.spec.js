/* eslint-disable no-undef */

describe('Calculate statistic', function() {
  // var obj;
  //
  // beforeEach(function() {
  //   obj = { };
  // });

  it('1 is 1', function() {
    expect(1).toEqual(1);
  });
});
